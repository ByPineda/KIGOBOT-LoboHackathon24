const { createBot, createProvider, createFlow, addKeyword, addAnswer } = require('@bot-whatsapp/bot')

const flowUsuarioNoValido = addAnswer('🚫 No tienes cuenta con Kigo, descagra la app. https://parkimovil.com/app?qr=EZZZ')